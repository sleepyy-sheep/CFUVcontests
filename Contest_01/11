#include <iostream>
#include <math.h>
using namespace std;

int main() {
	int a, b, c;
	cin >> a >> b >> c;
	if (abs(a - b) > abs(a - c)) {
		cout << "C" << " "<< abs(a - c);
	}
	else {
		cout << "B" <<" " <<abs(a - b);
	}
	return 0;
	  
}
